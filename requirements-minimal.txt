# Django Web Framework
Django>=5.0
python-decouple

# Utilities
requests>=2.31.0
python-dotenv
markdown

# Chatbot & AI Integration
google-generativeai>=0.3.0
PyYAML

# Minimal ML dependencies - only what's actually used in views.py
# TensorFlow CPU only (compatible with existing model)
tensorflow-cpu==2.13.0

# OpenCV headless (no GUI dependencies)
opencv-python-headless

# MediaPipe for pose detection
mediapipe

# Basic data processing
numpy
matplotlib
Pillow

# Additional utilities
python-dateutil
cryptography
djangorestframework

# No ultralytics, no torch, no scikit-learn, no pandas
# These were causing the 900MB+ downloads
